import { Router } from "express";
//TOFUCKINGDO
export const newUsers = express.Router();

users.Router("/users", (req, res) => {
  console.log("hello user");
});

Router.get("/users", (req, res) => {
  console.log("is this correct?");
});
